# baibaiAIGC

baibaiAIGC

## 使用说明

在使用本项目进行降 AIGC 处理之前，请先将需要处理的原始文件放入工作区根目录下的 `origin/` 文件夹。

- 原始输入文件默认从 `origin/` 目录读取。
- 如果 `origin/` 目录中不存在对应原始文件，使用时应先让用户上传文件，或提示用户先将文件放入 `origin/` 后再执行。
- 处理完成后的结果文件统一输出到工作区根目录下的 `finish/` 文件夹。
